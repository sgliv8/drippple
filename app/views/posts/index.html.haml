- if user_signed_in?
	%p#intro
		Hello
		= current_user.first_name
		%br/
		%span
			Share your inspiration and see what's inspiring others.
- else
	%p#intro
		what's your drippple?
		%br/
		%span
			Share your inspiration and see what's inspiring others.

#posts
	- @posts.each do |post|
		.post
			.post_image
				= link_to (image_tag post.image.url(:thumb)), post
			.post_content
				.title
					%h2= link_to post.title, post
				.data.clearfix
					%p.username
						Shared by
						= post.user.profile_name
					%p.buttons
						%span
							%i.fa.fa-comments-o
							= post.comments.count
						%span
							%i.fa.fa-thumbs-o-up
							= post.get_likes.size